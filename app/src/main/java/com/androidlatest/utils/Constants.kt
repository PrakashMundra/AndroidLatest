package com.androidlatest.utils

class Constants {
    companion object {
        const val BASE_URL = "https://static.westwing.de/"
        const val CONNECT_TIMEOUT: Long = 30
        const val READ_TIMEOUT: Long = 30
        const val WRITE_TIMEOUT: Long = 30
        const val DATABASE_NAME = "AndroidLatest.db"
        const val WORKER_CAMPAIGN_KEY = "campaign"
    }
}